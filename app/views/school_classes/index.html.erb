<h2>Classes</h2>

<div>
  <%= link_to "Create a New Class", new_school_class_path %>
</div><br>

<div>
  <ul>
    <% @school_classes.each do |school_class| %>
      <li><%= link_to "#{school_class.title} -- Room #{school_class.room_number}", school_class_path(school_class)%></li>
    <% end %>
  </ul>

</div>
